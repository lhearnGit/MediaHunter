export enum DiscoverMovieKeys {
  country = "with_origin_country",
  genres = "with_genres",
  release_year = "primary_release_year",
  page = "page",
}
export enum DiscoverTVKeys {
  network = "with_networks", //integer
  genres = "with_genres",
  first_air = "first_air_date_year",
  page = "page",
}
